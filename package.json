{
  "private": true,
  "scripts": {
    "bump": "node scripts/bump-version.js",
    "install:packages": "npm ci --prefix runner-package && npm ci --prefix auth-authme-package",
    "build:packages": "npm run build --prefix runner-package && npm run build --prefix auth-authme-package",
    "publish:packages": "node scripts/publish.js"
  }
}
